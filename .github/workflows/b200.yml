name: Meta Triton B200 Tests
on:
  push:
    branches:
      - main
    paths:
      - '**'
      - '!**/*.md'
      - '!docs/**'
      - '!website/**'
      - '!third_party/amd/**'
      - '!third_party/tlx/tutorials/gfx9_gemm/**'
      - '!third_party/amd/python/test/**'
      - '!python/test/unit/language/test_tlx_*_gfx942.py'
      - '!python/test/unit/language/test_tlx_*_gfx950.py'
      - '!python/test/unit/language/test_tlx_*_gfx1250.py'
      - '!python/test/unit/language/test_tlx_*_hip.py'
      - '!python/test/unit/tlx_ops/*_gfx950.py'
      - '!python/test/unit/tlx_ops/*_gfx942.py'
      - '!python/test/unit/tlx_ops/*_gfx1250.py'
      - '!python/test/unit/tlx_ops/*_hip.py'
      - '!python/test/unit/tlx_ops/*_sm90.py'
      - '!python/test/unit/language/test_tlx_*_sm90.py'
      # Agent instructions and skills do not affect generated code.
      - '!.agents/**'
      - '!.claude/**'
      - '!.llms/**'
      - '!AGENTS.md'
      - '!third_party/tlx/tools/agents/skills/**'
  pull_request:
    paths:
      - '**'
      - '!**/*.md'
      - '!docs/**'
      - '!website/**'
      - '!third_party/amd/**'
      - '!third_party/tlx/tutorials/gfx9_gemm/**'
      - '!third_party/amd/python/test/**'
      - '!python/test/unit/language/test_tlx_*_gfx942.py'
      - '!python/test/unit/language/test_tlx_*_gfx950.py'
      - '!python/test/unit/language/test_tlx_*_gfx1250.py'
      - '!python/test/unit/language/test_tlx_*_hip.py'
      - '!python/test/unit/tlx_ops/*_gfx950.py'
      - '!python/test/unit/tlx_ops/*_gfx942.py'
      - '!python/test/unit/tlx_ops/*_gfx1250.py'
      - '!python/test/unit/tlx_ops/*_hip.py'
      - '!python/test/unit/tlx_ops/*_sm90.py'
      - '!python/test/unit/language/test_tlx_*_sm90.py'
      # Agent instructions and skills do not affect generated code.
      - '!.agents/**'
      - '!.claude/**'
      - '!.llms/**'
      - '!AGENTS.md'
      - '!third_party/tlx/tools/agents/skills/**'
  schedule:
    - cron: '30 */6 * * *'

env:
  MAX_JOBS: 16
  TORCHINDUCTOR_TLX_MODE: "off"
  CCACHE_DIR: /tmp/fbtriton-ccache
  CCACHE_MAXSIZE: 2G

jobs:
  b200-build:
    if: github.repository_owner == 'facebookexperimental'
    runs-on: nvidia-dgx-b200
    env:
      CONDA_ENV: meta-triton
      SETUP_SCRIPT: /workspace/setup_instance.sh
    timeout-minutes: 60
    permissions:
      id-token: write
      contents: read
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Restore ccache
        uses: actions/cache@v4
        with:
          path: /tmp/fbtriton-ccache
          key: ccache-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            ccache-${{ github.job }}-
            ccache-
      - name: Compile Triton
        run: |
          . "${SETUP_SCRIPT}"
          . /workspace/tritonbench/.ci/triton/triton_install_utils.sh
          install_triton $PWD
      - name: Build wheel
        run: |
          . "${SETUP_SCRIPT}"
          uv build --wheel --no-build-isolation --out-dir dist/
          ls -la dist/
          python3 -c "import glob,sys,zipfile; w=glob.glob('dist/*.whl'); sys.exit('no wheel built') if len(w)!=1 else None; zipfile.ZipFile(w[0]).testzip(); print('wheel ok:', w[0])"
      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: triton-wheel-b200-${{ github.sha }}
          path: dist/*.whl
          retention-days: 1

  b200-meta-triton-test:
    needs: b200-build
    if: "!cancelled() && github.repository_owner == 'facebookexperimental'"
    runs-on: nvidia-dgx-b200
    env:
      CONDA_ENV: meta-triton
      SETUP_SCRIPT: /workspace/setup_instance.sh
    timeout-minutes: 180
    permissions:
      id-token: write
      contents: read
      actions: read
    outputs:
      failures: ${{ steps.collect.outputs.failures }}
    steps:
      - name: Fail if the build job did not succeed
        if: needs.b200-build.result != 'success'
        run: |
          echo "b200-build result: ${{ needs.b200-build.result }}"
          exit 1
      - name: Checkout
        uses: actions/checkout@v3
      - name: Tune Nvidia GPU
        run: |
          sudo nvidia-smi -pm 1
          sudo ldconfig
          nvidia-smi
      - name: Download prebuilt Triton wheel
        uses: actions/download-artifact@v4
        with:
          name: triton-wheel-b200-${{ github.sha }}
          path: ${{ runner.temp }}/prebuilt-wheel
      - name: Install Triton
        run: |
          . "${SETUP_SCRIPT}"
          uv pip install --force-reinstall --no-deps ${RUNNER_TEMP}/prebuilt-wheel/*.whl
      - name: Install torchao
        timeout-minutes: 10
        run: |
          . "${SETUP_SCRIPT}"
          uv pip install --pre torchao --index-url https://download.pytorch.org/whl/nightly/cu128
          python -c 'from torchao.prototype.mx_formats.mx_tensor import MXTensor, ScaleCalculationMode'
      - name: Export TritonBench env
        run: |
          . "${SETUP_SCRIPT}"
          set -x
          TRITONBENCH_TRITON_COMMIT_HASH=$(git rev-parse --verify HEAD)
          TRITONBENCH_TRITON_REPO=$(git config --get remote.origin.url | sed -E 's|.*github.com[:/](.+)\.git|\1|')
          TRITONBENCH_TRITON_COMMIT=${GITHUB_REF_NAME}
          TRITONBENCH_INSTALL_DIR=${PWD}
          cat <<EOF >> "${SETUP_SCRIPT}"
          if [ \${CONDA_ENV} == "${CONDA_ENV}" ] ; then
              export TRITONBENCH_TRITON_COMMIT_HASH="${TRITONBENCH_TRITON_COMMIT_HASH}"
              export TRITONBENCH_TRITON_REPO="${TRITONBENCH_TRITON_REPO}"
              export TRITONBENCH_TRITON_COMMIT="${TRITONBENCH_TRITON_COMMIT}"
              export TRITONBENCH_TRITON_INSTALL_DIR="${TRITONBENCH_INSTALL_DIR}"
          fi
          EOF
      - name: Checkout TritonBench (Optional)
        if: github.event_name == 'pull_request'
        env:
          PR_BODY: ${{ github.event.pull_request.body }}
        run: |
          PR_BODY_FILE="$(mktemp)"
          printf '%s' "${PR_BODY}" > "${PR_BODY_FILE}"
          bash ./.ci/tritonbench/checkout.sh "${PR_BODY_FILE}"
      - name: Run TritonBench tests on B200 GPU
        id: tritonbench
        env:
          TRITONBENCH_TEST_SKIP_FILE: ${{ github.workspace }}/.ci/tritonbench/fbtriton_skip_tests.yaml
        run: |
          . "${SETUP_SCRIPT}"
          export TRITONBENCH_ROOT="${TRITONBENCH_ROOT:-/workspace/tritonbench}"
          bash ./.ci/tritonbench/test-gpu.sh
      - name: Collect nightly failure (TritonBench)
        id: collect
        if: always() && github.event_name == 'schedule' && failure()
        run: |
          BUCKET="b200-tritonbench"
          TITLE="[nightly] ${GITHUB_WORKFLOW} / b200-meta-triton-test / ${BUCKET}"
          FAILURES='[{"normalized_failure_id":"'"${BUCKET}"'","raw_failure_ids":"","issue_title":"'"${TITLE}"'","job_name":"b200-meta-triton-test","summary":"TritonBench tests failed on B200 (see run log).","repro":"","external_dep":false,"external_dep_reason":""}]'
          echo "failures=${FAILURES}" >> "$GITHUB_OUTPUT"

  b200-tlx-test:
    needs: b200-build
    if: "!cancelled() && github.repository_owner == 'facebookexperimental'"
    runs-on: nvidia-dgx-b200
    env:
      CONDA_ENV: meta-triton
      SETUP_SCRIPT: /workspace/setup_instance.sh
    timeout-minutes: 240
    permissions:
      id-token: write
      contents: read
      actions: read
    outputs:
      failures: ${{ steps.parse.outputs.failures }}
    steps:
      - name: Fail if the build job did not succeed
        if: needs.b200-build.result != 'success'
        run: |
          echo "b200-build result: ${{ needs.b200-build.result }}"
          exit 1
      - name: Checkout
        uses: actions/checkout@v3
      - name: Tune Nvidia GPU
        run: |
          sudo nvidia-smi -pm 1
          sudo ldconfig
          nvidia-smi
      - name: Download prebuilt Triton wheel
        uses: actions/download-artifact@v4
        with:
          name: triton-wheel-b200-${{ github.sha }}
          path: ${{ runner.temp }}/prebuilt-wheel
      - name: Install Triton
        run: |
          . "${SETUP_SCRIPT}"
          uv pip install --force-reinstall --no-deps ${RUNNER_TEMP}/prebuilt-wheel/*.whl
          uv pip install pytest
      - name: Install torchao
        timeout-minutes: 10
        run: |
          . "${SETUP_SCRIPT}"
          uv pip install --pre torchao --index-url https://download.pytorch.org/whl/nightly/cu128
          python -c 'from torchao.prototype.mx_formats.mx_tensor import MXTensor, ScaleCalculationMode'
      - name: Run TLX core tests
        id: run_core_tests
        timeout-minutes: 90
        run: |
          . "${SETUP_SCRIPT}"
          shopt -s nullglob
          FILES=(
            python/test/unit/language/test_tlx_*_any.py
            python/test/unit/language/test_tlx_*sm90up*.py
            python/test/unit/language/test_tlx_*sm100*.py
            python/test/unit/tlx_ops/test_*_sm100.py
          )
          # TorchTLX depends on floating PyTorch Inductor APIs and runs in the
          # separate b200-torchtlx-test job below, not this pinned-PyTorch job.
          TLX_FILES=()
          for file in "${FILES[@]}"; do
            [[ "${file}" == */test_torchtlx_* ]] || TLX_FILES+=("${file}")
          done
          FILES=("${TLX_FILES[@]}")
          [ ${#FILES[@]} -gt 0 ] || { echo "no TLX core test files matched"; exit 1; }
          echo "b200 collects:" "${FILES[@]}"
          python -m pytest "${FILES[@]}" -v --junitxml=/tmp/tlx-core.xml
      - name: Run TLX tutorial correctness tests
        id: run_correctness_tests
        timeout-minutes: 30
        run: |
          . "${SETUP_SCRIPT}"
          python -m pytest third_party/tlx/tutorials/testing/test_correctness.py -v --junitxml=/tmp/tlx-correctness.xml
      - name: Run device-agnostic TLX codegen tests
        id: run_codegen_tests
        timeout-minutes: 30
        run: |
          . "${SETUP_SCRIPT}"
          python -m pytest python/test/unit/language/test_tlx_codegen.py -v --junitxml=/tmp/tlx-codegen.xml
      - name: Run host-default TLX L2 perf
        id: run_perf_tests
        if: always() && github.event_name == 'schedule'
        timeout-minutes: 60
        run: |
          . "${SETUP_SCRIPT}"
          python -m pytest python/test/tlx_benchmark/test_ops_perf.py \
            -k "not torchtlx" -v --junitxml=/tmp/tlx-perf.xml
      - name: Upload TLX perf artifacts
        if: always() && github.event_name == 'schedule'
        uses: actions/upload-artifact@v4
        with:
          name: b200-tlx-perf
          path: /tmp/tlx_benchmark/*.json
          if-no-files-found: ignore
      - name: Collect nightly failures (TLX)
        id: parse
        if: always() && github.event_name == 'schedule'
        run: |
          . "${SETUP_SCRIPT}"
          FAILED_FLAG=""
          if [[ "${{ steps.run_core_tests.outcome }}" != "success" || "${{ steps.run_correctness_tests.outcome }}" != "success" || "${{ steps.run_codegen_tests.outcome }}" != "success" || "${{ steps.run_perf_tests.outcome }}" != "success" ]]; then
            FAILED_FLAG="--failed --bucket b200-tlx-job-failed"
          fi
          python3 .github/scripts/parse_junit_failures.py \
            --junit /tmp/tlx-core.xml /tmp/tlx-correctness.xml /tmp/tlx-codegen.xml /tmp/tlx-perf.xml \
            --budget /tmp/tlx-core.xml=90 --budget /tmp/tlx-correctness.xml=30 --budget /tmp/tlx-codegen.xml=30 --budget /tmp/tlx-perf.xml=60 \
            --workflow "${GITHUB_WORKFLOW}" \
            --job "b200-tlx-test" \
            ${FAILED_FLAG}

  b200-torchtlx-test:
    # Temporarily disabled because the TorchTLX environment is brittle.
    if: ${{ false }}
    runs-on: nvidia-dgx-b200
    env:
      CONDA_ENV: meta-triton
      SETUP_SCRIPT: /workspace/setup_instance.sh
      TORCHINDUCTOR_TLX_MODE: "allow"
      TORCHTLX_ARCH: sm100
    timeout-minutes: 90
    permissions:
      id-token: write
      contents: read
    outputs:
      failures: ${{ steps.parse.outputs.failures }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Tune Nvidia GPU
        run: |
          sudo nvidia-smi -pm 1
          sudo ldconfig
          nvidia-smi
      - name: Create isolated TorchTLX environment
        run: |
          set -eux
          . "${SETUP_SCRIPT}"
          BASE_PYTHON="$(command -v python3)"
          BASE_SITE="$("${BASE_PYTHON}" -c 'import site; print(site.getsitepackages()[0])')"
          TORCHTLX_VENV="${RUNNER_TEMP}/torchtlx-venv"

          # The runner's meta-triton environment carries Meta's refreshed PyTorch.
          # Expose it read-only to a job-local venv; installs below then land in the
          # venv and shadow bundled Triton without mutating the shared environment.
          uv venv --python "${BASE_PYTHON}" "${TORCHTLX_VENV}"
          TORCHTLX_PYTHON="${TORCHTLX_VENV}/bin/python"
          TORCHTLX_SITE="$("${TORCHTLX_PYTHON}" -c 'import site; print(site.getsitepackages()[0])')"
          printf '%s\n' "${BASE_SITE}" > "${TORCHTLX_SITE}/meta-triton.pth"

          "${TORCHTLX_PYTHON}" - <<'PY'
          import torch
          from torch._inductor import config

          print(f"torch -> {torch.__version__} ({torch.__file__})")
          if not hasattr(config.triton, "tlx_mode"):
              raise RuntimeError(
                  "the pre-provisioned meta-triton PyTorch has no TorchTLX "
                  "Inductor support (torch._inductor.config.triton.tlx_mode)"
              )
          PY
      - name: Compile Triton
        run: |
          set -eux
          . "${SETUP_SCRIPT}"
          TORCHTLX_PYTHON="${RUNNER_TEMP}/torchtlx-venv/bin/python"
          uv pip install --python "${TORCHTLX_PYTHON}" \
            -r python/requirements.txt -r python/test-requirements.txt
          uv pip install --python "${TORCHTLX_PYTHON}" --reinstall --no-deps \
            -e . --no-build-isolation
          uv pip install --python "${TORCHTLX_PYTHON}" pytest expecttest
          "${TORCHTLX_PYTHON}" - <<'PY'
          import torch
          import triton

          print(f"torch  -> {torch.__version__} ({torch.__file__})")
          print(f"triton -> {triton.__file__}")
          PY
      - name: Run TorchTLX template + fusion tests
        id: run_torchtlx_tests
        timeout-minutes: 40
        run: |
          . "${SETUP_SCRIPT}"
          export PYTHON="${RUNNER_TEMP}/torchtlx-venv/bin/python"
          bash scripts/run_torchtlx_tests.sh --junitxml=/tmp/torchtlx.xml
      - name: Run TorchTLX mm perf bench
        id: run_torchtlx_perf
        if: always() && github.event_name == 'schedule'
        timeout-minutes: 40
        run: |
          . "${SETUP_SCRIPT}"
          export PYTHON="${RUNNER_TEMP}/torchtlx-venv/bin/python"
          bash scripts/run_torchtlx_tests.sh --perf \
            --json /tmp/mm_torchtlx.json --junitxml=/tmp/torchtlx_perf.xml
      - name: Upload TorchTLX perf artifact
        if: always() && github.event_name == 'schedule'
        uses: actions/upload-artifact@v4
        with:
          name: b200-torchtlx-mm-perf
          path: /tmp/mm_torchtlx.json
          if-no-files-found: ignore
      - name: Collect nightly failures (TorchTLX)
        id: parse
        if: always() && github.event_name == 'schedule'
        run: |
          . "${SETUP_SCRIPT}"
          FAILED_FLAG=""
          if [[ "${{ steps.run_torchtlx_tests.outcome }}" != "success" || \
                "${{ steps.run_torchtlx_perf.outcome }}" != "success" ]]; then
            FAILED_FLAG="--failed --bucket b200-torchtlx-job-failed"
          fi
          python3 .github/scripts/parse_junit_failures.py \
            --junit /tmp/torchtlx.xml /tmp/torchtlx_perf.xml \
            --workflow "${GITHUB_WORKFLOW}" \
            --job "b200-torchtlx-test" \
            ${FAILED_FLAG}

  b200-gluon-test:
    needs: b200-build
    if: "!cancelled() && github.repository_owner == 'facebookexperimental'"
    runs-on: nvidia-dgx-b200
    env:
      CONDA_ENV: meta-triton
      SETUP_SCRIPT: /workspace/setup_instance.sh
    timeout-minutes: 60
    permissions:
      id-token: write
      contents: read
      actions: read
    outputs:
      failures: ${{ steps.parse.outputs.failures }}
    steps:
      - name: Fail if the build job did not succeed
        if: needs.b200-build.result != 'success'
        run: |
          echo "b200-build result: ${{ needs.b200-build.result }}"
          exit 1
      - name: Checkout
        uses: actions/checkout@v3
      - name: Tune Nvidia GPU
        run: |
          sudo nvidia-smi -pm 1
          sudo ldconfig
          nvidia-smi
      - name: Download prebuilt Triton wheel
        uses: actions/download-artifact@v4
        with:
          name: triton-wheel-b200-${{ github.sha }}
          path: ${{ runner.temp }}/prebuilt-wheel
      - name: Install Triton
        run: |
          . "${SETUP_SCRIPT}"
          uv pip install --force-reinstall --no-deps ${RUNNER_TEMP}/prebuilt-wheel/*.whl
          uv pip install pytest expecttest
      - name: Run Gluon tests (python/test/gluon)
        id: run_gluon
        timeout-minutes: 50
        run: |
          . "${SETUP_SCRIPT}"
          python -m pytest python/test/gluon/ -v --junitxml=/tmp/gluon.xml
      - name: Collect nightly failures (Gluon)
        id: parse
        if: always() && github.event_name == 'schedule'
        run: |
          . "${SETUP_SCRIPT}"
          FAILED_FLAG=""
          if [[ "${{ steps.run_gluon.outcome }}" != "success" ]]; then
            FAILED_FLAG="--failed --bucket b200-gluon-job-failed"
          fi
          python3 .github/scripts/parse_junit_failures.py \
            --junit /tmp/gluon.xml \
            --budget /tmp/gluon.xml=50 \
            --workflow "${GITHUB_WORKFLOW}" \
            --job "b200-gluon-test" \
            ${FAILED_FLAG}

  report-b200-tritonbench:
    needs: b200-meta-triton-test
    if: always() && github.event_name == 'schedule' && needs.b200-meta-triton-test.outputs.failures != '' && needs.b200-meta-triton-test.outputs.failures != '[]'
    permissions:
      contents: read
      issues: write
      actions: read
    strategy:
      fail-fast: false
      matrix:
        item: ${{ fromJSON(needs.b200-meta-triton-test.outputs.failures) }}
    uses: ./.github/workflows/report-nightly-failure.yml
    with:
      issue_title: ${{ matrix.item.issue_title }}
      normalized_failure_id: ${{ matrix.item.normalized_failure_id }}
      raw_failure_ids: ${{ matrix.item.raw_failure_ids }}
      workflow_name: ${{ github.workflow }}
      job_name: ${{ matrix.item.job_name }}
      run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      ref: ${{ github.ref }}
      sha: ${{ github.sha }}
      event_name: ${{ github.event_name }}
      run_attempt: ${{ github.run_attempt }}
      summary: ${{ matrix.item.summary }}
      repro: ${{ matrix.item.repro }}
      external_dep: ${{ matrix.item.external_dep }}
      external_dep_reason: ${{ matrix.item.external_dep_reason }}
      platform: b200

  report-b200-tlx:
    needs: b200-tlx-test
    if: always() && github.event_name == 'schedule' && needs.b200-tlx-test.outputs.failures != '' && needs.b200-tlx-test.outputs.failures != '[]'
    permissions:
      contents: read
      issues: write
      actions: read
    strategy:
      fail-fast: false
      matrix:
        item: ${{ fromJSON(needs.b200-tlx-test.outputs.failures) }}
    uses: ./.github/workflows/report-nightly-failure.yml
    with:
      issue_title: ${{ matrix.item.issue_title }}
      normalized_failure_id: ${{ matrix.item.normalized_failure_id }}
      raw_failure_ids: ${{ matrix.item.raw_failure_ids }}
      workflow_name: ${{ github.workflow }}
      job_name: ${{ matrix.item.job_name }}
      run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      ref: ${{ github.ref }}
      sha: ${{ github.sha }}
      event_name: ${{ github.event_name }}
      run_attempt: ${{ github.run_attempt }}
      summary: ${{ matrix.item.summary }}
      repro: ${{ matrix.item.repro }}
      external_dep: ${{ matrix.item.external_dep }}
      external_dep_reason: ${{ matrix.item.external_dep_reason }}
      platform: b200

  report-b200-torchtlx:
    needs: b200-torchtlx-test
    if: always() && github.event_name == 'schedule' && needs.b200-torchtlx-test.outputs.failures != '' && needs.b200-torchtlx-test.outputs.failures != '[]'
    permissions:
      contents: read
      issues: write
      actions: read
    strategy:
      fail-fast: false
      matrix:
        item: ${{ fromJSON(needs.b200-torchtlx-test.outputs.failures) }}
    uses: ./.github/workflows/report-nightly-failure.yml
    with:
      issue_title: ${{ matrix.item.issue_title }}
      normalized_failure_id: ${{ matrix.item.normalized_failure_id }}
      raw_failure_ids: ${{ matrix.item.raw_failure_ids }}
      workflow_name: ${{ github.workflow }}
      job_name: ${{ matrix.item.job_name }}
      run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      ref: ${{ github.ref }}
      sha: ${{ github.sha }}
      event_name: ${{ github.event_name }}
      run_attempt: ${{ github.run_attempt }}
      summary: ${{ matrix.item.summary }}
      repro: ${{ matrix.item.repro }}

  report-b200-gluon:
    needs: b200-gluon-test
    if: always() && github.event_name == 'schedule' && needs.b200-gluon-test.outputs.failures != '' && needs.b200-gluon-test.outputs.failures != '[]'
    permissions:
      contents: read
      issues: write
      actions: read
    strategy:
      fail-fast: false
      matrix:
        item: ${{ fromJSON(needs.b200-gluon-test.outputs.failures) }}
    uses: ./.github/workflows/report-nightly-failure.yml
    with:
      issue_title: ${{ matrix.item.issue_title }}
      normalized_failure_id: ${{ matrix.item.normalized_failure_id }}
      raw_failure_ids: ${{ matrix.item.raw_failure_ids }}
      workflow_name: ${{ github.workflow }}
      job_name: ${{ matrix.item.job_name }}
      run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      ref: ${{ github.ref }}
      sha: ${{ github.sha }}
      event_name: ${{ github.event_name }}
      run_attempt: ${{ github.run_attempt }}
      summary: ${{ matrix.item.summary }}
      repro: ${{ matrix.item.repro }}
      external_dep: ${{ matrix.item.external_dep }}
      external_dep_reason: ${{ matrix.item.external_dep_reason }}
      platform: b200

  reconcile-b200-tritonbench:
    needs: b200-meta-triton-test
    if: always() && github.event_name == 'schedule' && (needs.b200-meta-triton-test.result == 'success' || needs.b200-meta-triton-test.result == 'failure')
    permissions:
      contents: read
      issues: write
    uses: ./.github/workflows/reconcile-nightly-issues.yml
    with:
      workflow_name: ${{ github.workflow }}
      job_name: b200-meta-triton-test
      failures: ${{ needs.b200-meta-triton-test.outputs.failures }}
      run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

  reconcile-b200-tlx:
    needs: b200-tlx-test
    if: always() && github.event_name == 'schedule' && (needs.b200-tlx-test.result == 'success' || needs.b200-tlx-test.result == 'failure')
    permissions:
      contents: read
      issues: write
    uses: ./.github/workflows/reconcile-nightly-issues.yml
    with:
      workflow_name: ${{ github.workflow }}
      job_name: b200-tlx-test
      failures: ${{ needs.b200-tlx-test.outputs.failures }}
      run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

  reconcile-b200-torchtlx:
    needs: b200-torchtlx-test
    if: always() && github.event_name == 'schedule' && (needs.b200-torchtlx-test.result == 'success' || needs.b200-torchtlx-test.result == 'failure')
    permissions:
      contents: read
      issues: write
    uses: ./.github/workflows/reconcile-nightly-issues.yml
    with:
      workflow_name: ${{ github.workflow }}
      job_name: b200-torchtlx-test
      failures: ${{ needs.b200-torchtlx-test.outputs.failures }}
      run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

  reconcile-b200-gluon:
    needs: b200-gluon-test
    if: always() && github.event_name == 'schedule' && (needs.b200-gluon-test.result == 'success' || needs.b200-gluon-test.result == 'failure')
    permissions:
      contents: read
      issues: write
    uses: ./.github/workflows/reconcile-nightly-issues.yml
    with:
      workflow_name: ${{ github.workflow }}
      job_name: b200-gluon-test
      failures: ${{ needs.b200-gluon-test.outputs.failures }}
      run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}-${{ github.event_name == 'pull_request' }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
